spring:
  application:
    name: springboot-jopas-test
    version: 1.1.0

  profiles:
    active: dev
  
  config.import: "optional:configserver:"
  cloud:
    config:
      name: springboot-jopas-test
      enabled: true
      uri: http://192.168.1.135:8888